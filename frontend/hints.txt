 Hints:
   index.css file was edited.
   hooks were used to keep the state (index.js/home.js)  , remove <WorkoutsContextProvider> from index.js and certain codes from home.js to run as default way
 
 
 
 
 Todo :
    Install react router dom package to add diff pages to this application > npm install react-router-dom
    Add "proxy": "http://localhost:4000", in package.json to avoid CORS Policy
    Add <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@48,400,0,0" /> in to public>index.html
    Install date-fns for change date as we want > npm install date-fns